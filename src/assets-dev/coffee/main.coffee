###
# + Main script
# =====================================================================
###
do ($ = jQuery, document, window) ->
    'use strict'

    ###
    # + Dom ready
    # =====================================================================
    ###
    $ ->
        console.log 'DOM ready'
        return

    # = Dom ready

    return

# = Main script
